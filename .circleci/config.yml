version: 2.1
workflows:
  testing:
    jobs:
      - runner-test
jobs:
  runner-test:
    machine: true
    resource_class: yuji-hatakeyama/hello-test
    parallelism: 3
    steps:
      - run: echo "Hi I'm on Runners!"
      - run: whoami
      - run: date
      - run: hostname
